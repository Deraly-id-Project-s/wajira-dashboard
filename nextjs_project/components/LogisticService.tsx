import React from "react";
import { motion } from "framer-motion";

export default function LogisticService(): React.JSX.Element {
    return (
        <section id="logistic-service" className="flex flex-col items-center justify-center py-16 px-4 max-w-6xl mx-auto">
            <h2 className="text-3xl font-bold mb-4 text-center">
                Our Logistic Services
            </h2>
        </section>
    )
}